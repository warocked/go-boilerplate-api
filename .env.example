# ============================================
# Go Boilerplate API - Environment Variables
# ============================================
# Copy this file to .env and update with your actual values
# Never commit .env file to version control

# ============================================
# Server Configuration
# ============================================
# Port on which the server will listen
PORT=8080

# Environment mode: set to "true" for production
IS_PROD=false

# Log level: debug, info, warn, error, fatal
# Production should use: info, warn, or error
LOG_LEVEL=info

# Timezone for date operations (IANA timezone database format)
# Examples: Asia/Manila, America/New_York, Europe/London, UTC
TIMEZONE=Asia/Manila

# ============================================
# Security Configuration
# ============================================
# Secret key for JWT token signing and encryption
# IMPORTANT: Generate a strong, random secret key (min 32 characters)
# Use: openssl rand -hex 32 or similar to generate
SECRET_KEY=your-super-secret-key-change-this-in-production-minimum-32-characters

# Token expiration time (duration format: 1h, 30m, 24h, etc.)
TOKEN_EXPIRE_TIME=5h

# ============================================
# CORS Configuration
# ============================================
# Allowed origins for CORS (comma-separated for multiple origins)
# Example: https://example.com,https://www.example.com
# IMPORTANT: Cannot use "*" when AllowCredentials is enabled. Must specify exact origins.
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# Database Configuration
# ============================================
# PostgreSQL database connection string
# Format: postgres://username:password@host:port/database?sslmode=require
# For production, always use SSL/TLS (sslmode=require or sslmode=verify-full)
DATABASE_URL=postgres://username:password@localhost:5432/dbname?sslmode=require

# ============================================
# Redis Configuration (Optional)
# Leave REDIS_URL empty if you do not want to use Redis

# Redis connection URL
# Format: redis://username:password@host:port/db
# For production with authentication: redis://username:password@host:6379/0
# For production with TLS: rediss://username:password@host:6380/0
# REDIS_URL=redis://localhost:6379/0

# Redis keys TTL (Time To Live) duration
# Format: duration format (24h, 7d, 168h, etc.)
REDIS_KEYS_TTL=168h

# ============================================
# AWS S3 Configuration (Optional)
# ============================================
# S3 bucket name for file storage

# Request body limit for uploads (in MB)
# Note: This only affects request bodies (uploads), not response bodies (downloads)
# Default: 50MB. Increase if you need to upload larger files.
REQUEST_BODY_LIMIT_MB=100
S3BUCKETNAME=your-s3-bucket-name

# ============================================
# Production Checklist
# ============================================
# [ ] IS_PROD set to "true"
# [ ] LOG_LEVEL set to "info" or higher (not "debug")
# [ ] SECRET_KEY changed to a strong, random value
# [ ] DATABASE_URL uses SSL (sslmode=require)
# [ ] REDIS_URL uses authentication and/or TLS in production
# [ ] ALLOWED_ORIGINS specifies exact domains (not "*")
# [ ] All credentials are secure and not committed to git
# [ ] Environment variables are set in your deployment platform